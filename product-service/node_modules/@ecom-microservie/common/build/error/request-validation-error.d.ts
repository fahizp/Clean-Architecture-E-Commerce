import { CustomError } from "./custom-error";
interface CustomValidationError {
    param?: string;
    msg?: string;
}
export declare class RequestValidationError extends CustomError {
    errors: CustomValidationError[];
    statusCode: number;
    constructor(errors: CustomValidationError[]);
    serializeErrors(): {
        message: string;
        field?: string;
    }[];
}
export {};
